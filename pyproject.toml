[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pagerank"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "networkx",
]

[tool.setuptools]
packages = ["pagerank"]
